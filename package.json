{
  "private": true,
  "workspaces": {
    "packages": ["apps/*"],
    "nohoist": ["**/lucide-react"]
  },
  "scripts": {
    "dev": "npm-run-all --parallel client server",
    "client": "yarn --cwd apps/frontend dev",
    "server": "yarn --cwd apps/backend dev"
  },
  "devDependencies": {
    "nodemon": "^3.0.3",
    "npm-run-all": "^4.1.5"
  }
}
